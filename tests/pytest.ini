[pytest]
# Lives in tests/ so the rootdir is tests/: the plugin root carries an __init__.py (Hermes loads it as a
# package) and pytest would otherwise collect it as a Package and fail on its relative imports.
# Run: python -m pytest tests -q
